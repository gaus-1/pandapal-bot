# 🗄️ SQL Скрипты для базы данных PandaPal

## 📋 Описание

Профессиональные SQL скрипты для создания оптимизированной структуры базы данных PostgreSQL для образовательного бота PandaPal.

---

## 🚀 Быстрый старт

### Шаг 1: Удаление старых таблиц

1. Открой **pgAdmin 4**
2. Подключись к базе `pandapal_db`
3. Правой кнопкой на базе → **Query Tool** (или `Alt+Shift+Q`)
4. Открой файл `01_drop_all_tables.sql`
5. Нажми **Execute** (F5)

✅ **Результат:** Все старые таблицы удалены

---

### Шаг 2: Создание новых таблиц

1. В том же Query Tool
2. Открой файл `02_create_tables.sql`
3. Нажми **Execute** (F5)

✅ **Результат:** Созданы 8 оптимизированных таблиц

---

## 📊 Структура базы данных

### 1. **users** - Пользователи
- Дети и родители
- Связь родитель-ребенок
- Профиль: возраст, класс, настройки

### 2. **chat_history** - История чата
- Все сообщения с AI
- Типы: user, ai, system
- Подсчет токенов

### 3. **learning_sessions** - Учебные сессии
- Предмет, тема, сложность
- Статистика: вопросы, правильные ответы
- **Автоматический расчет:** точность, длительность

### 4. **user_progress** - Прогресс
- Уровень по каждому предмету
- Очки, достижения
- Средняя точность

### 5. **analytics_metrics** - Метрики
- Любые числовые показатели
- Теги для фильтрации
- Периоды: минута, час, день, неделя, месяц

### 6. **user_sessions** - Сессии
- Активность пользователя
- Оценки вовлеченности и безопасности
- Статистика сообщений

### 7. **user_events** - События
- Логирование важных событий
- Уровни важности: low, normal, high, critical
- Флаг обработки

### 8. **analytics_reports** - Отчеты
- Сгенерированные отчеты для родителей
- Данные в JSON формате
- Запланированные отчеты

---

## 🎯 Ключевые особенности

### ✅ Оптимизация производительности
- **Индексы** на всех часто используемых полях
- **Составные индексы** для сложных запросов
- **Партиционирование** готово для больших таблиц

### ✅ Целостность данных
- **Foreign Keys** с CASCADE для автоматического удаления
- **CHECK constraints** для валидации
- **UNIQUE constraints** для уникальности

### ✅ Автоматизация
- **Вычисляемые поля** (accuracy, duration)
- **Триггеры** для updated_at
- **DEFAULT значения** для всех полей

### ✅ Масштабируемость
- **BIGSERIAL** для больших объемов
- **JSONB** для гибких данных
- **Индексы** для быстрых запросов

---

## 📖 Примеры запросов

### Получить всех детей родителя
```sql
SELECT * FROM users
WHERE parent_telegram_id = 123456789;
```

### Последние 10 сообщений пользователя
```sql
SELECT * FROM chat_history
WHERE user_telegram_id = 123456789
ORDER BY timestamp DESC
LIMIT 10;
```

### Статистика по предметам
```sql
SELECT
    subject,
    COUNT(*) as sessions_count,
    AVG(accuracy) as avg_accuracy,
    SUM(duration_seconds) as total_time
FROM learning_sessions
WHERE user_telegram_id = 123456789
GROUP BY subject;
```

### Топ пользователей по очкам
```sql
SELECT
    u.first_name,
    up.subject,
    up.points,
    up.level
FROM user_progress up
JOIN users u ON u.telegram_id = up.user_telegram_id
ORDER BY up.points DESC
LIMIT 10;
```

---

## ⚠️ ВАЖНО

### Перед удалением таблиц:
1. ✅ Сделай **бэкап** базы данных
2. ✅ Убедись, что бот **остановлен**
3. ✅ Проверь, что это **правильная** база

### Бэкап в pgAdmin:
1. Правой кнопкой на базе `pandapal_db`
2. **Backup...**
3. Выбери формат: **Custom**
4. Сохрани файл

---

## 🔄 Миграции Alembic

После создания таблиц вручную, нужно синхронизировать Alembic:

```bash
# В корне проекта
alembic stamp head
```

Это отметит, что база данных в актуальном состоянии.

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверь логи в pgAdmin (Messages)
2. Убедись, что PostgreSQL 17 поддерживает все функции
3. Проверь права пользователя базы данных

---

**Создано для PandaPal Bot** 🐼
