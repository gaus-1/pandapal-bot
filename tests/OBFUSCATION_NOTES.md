# 📝 Заметки об обфусцированных файлах для тестов

## Обфусцированные файлы в проекте

### 1. Конфигурация (`bot/config/_obfuscated/`)
- `prompts.py` → обфусцированная версия с AI промптами
- `forbidden_patterns.py` → обфусцированная версия с паттернами модерации

### 2. Сервисы (`bot/services/_optimized/`)
- `moderation_service.py` → обфусцированная версия сервиса модерации

## Как это работает в тестах

### ✅ Автоматический fallback
Тесты используют импорты через обертки:
```python
from bot.config import FORBIDDEN_PATTERNS, AI_SYSTEM_PROMPT
from bot.services import ContentModerationService
```

**Механизм:**
1. Сначала пытается импортировать обфусцированные файлы
2. Если их нет → fallback на оригинальные файлы
3. Тесты работают в обоих случаях

### ✅ Локальная разработка
- Обфусцированные файлы обычно отсутствуют (в `.gitignore`)
- Тесты используют оригинальные файлы через fallback
- Всё работает корректно

### ✅ Production (Railway)
- Обфусцированные файлы создаются при build через `railway_build.sh`
- Тесты используют обфусцированные файлы (если доступны)
- Всё работает корректно

## Важно для тестов

### ✅ Не нужно мокать обфусцированные файлы
Тесты должны работать с реальными импортами:
```python
# ✅ ПРАВИЛЬНО
from bot.services import ContentModerationService
service = ContentModerationService()

# ❌ НЕПРАВИЛЬНО (не нужно)
from bot.services.moderation_service import ContentModerationService
```

### ✅ Тесты проверяют функциональность, а не реализацию
- Тесты проверяют что `ContentModerationService.is_safe_content()` работает
- Не важно, обфусцирован файл или нет
- Поведение одинаковое

### ✅ Обфусцированные файлы не влияют на тесты
- Все тесты проходят с оригинальными файлами (fallback)
- Все тесты проходят с обфусцированными файлами (если доступны)
- Функциональность идентична

## Проверка наличия обфусцированных файлов

Если нужно проверить, используются ли обфусцированные файлы:
```python
# В bot/config/__init__.py
_optimized_available = False
try:
    from bot.config._obfuscated import AI_SYSTEM_PROMPT, FORBIDDEN_PATTERNS
    _optimized_available = True
except ImportError:
    # Fallback на оригинальные
    pass
```

## Итог

✅ **Тесты учитывают обфусцированные файлы автоматически**
✅ **Не нужно ничего менять в тестах**
✅ **Fallback механизм работает прозрачно**
✅ **Все тесты проходят в обоих случаях**
