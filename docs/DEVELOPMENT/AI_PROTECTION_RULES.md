# 🛡️ Правила защиты рабочего кода

**⚠️ КРИТИЧЕСКИ ВАЖНО: ПРОЧИТАЙ ПЕРЕД ЛЮБЫМИ ИЗМЕНЕНИЯМИ!**

Этот документ содержит список модулей, которые **РАБОТАЮТ** и **НЕ ТРЕБУЮТ ИЗМЕНЕНИЙ**.

## 🔴 МОДУЛИ, КОТОРЫЕ НЕЛЬЗЯ ТРОГАТЬ БЕЗ КРИТИЧЕСКОЙ НЕОБХОДИМОСТИ

### 1. `web_server.py` - Entry point сервера
**Статус:** ✅ Работает 24/7 на Railway
**Риск:** Поломка = бот недоступен

**НЕ МЕНЯТЬ:**
- ❌ Структуру запуска (`main()`, `startup()`, `run()`)
- ❌ Порты и webhook настройки
- ❌ Раздачу статики frontend (`frontend/dist`)
- ❌ Регистрацию роутов и middleware

**МОЖНО:**
- ✅ Добавлять новые API endpoints
- ✅ Улучшать логирование
- ✅ Добавлять новые middleware

---

### 2. `bot/security/middleware.py` - Security headers
**Статус:** ✅ Работает, защищает от атак
**Риск:** Поломка = уязвимости безопасности

**НЕ МЕНЯТЬ:**
- ❌ `'unsafe-inline'` в `script-src` (нужно для Telegram WebView inline скриптов)
- ❌ `frame-ancestors` для Telegram доменов
- ❌ Rate limiting логику
- ❌ CSRF protection для API

**МОЖНО:**
- ✅ Добавлять новые проверки безопасности
- ✅ Улучшать логирование security событий

---

### 3. `bot/database.py` - Подключение к PostgreSQL
**Статус:** ✅ Работает, connection pool настроен
**Риск:** Поломка = потеря данных или недоступность БД

**НЕ МЕНЯТЬ:**
- ❌ Connection pool настройки
- ❌ Логику миграций (`AUTO_MIGRATE`)
- ❌ Проверки подключения
- ❌ Context manager `get_db()`

**МОЖНО:**
- ✅ Оптимизировать SQL запросы
- ✅ Добавлять новые индексы через миграции

---

### 4. `frontend/index.html` - HTML структура
**Статус:** ✅ Работает, favicon настроен, CSP правильный
**Риск:** Поломка = сайт не открывается или ошибки в консоли

**НЕ МЕНЯТЬ:**
- ❌ Inline скрипты для подавления ошибок Telegram WebView
- ❌ Favicon настройки (`/favicon.ico`, `/logo.png`)
- ❌ PWA мета-теги (они удалены намеренно)
- ❌ CSP настройки в HTML (если есть)

**МОЖНО:**
- ✅ Обновлять SEO мета-теги
- ✅ Добавлять новые скрипты (но проверь CSP в middleware)

---

### 5. `bot/config/prompts.py` - Промпты для генерации ответов
**Статус:** ✅ Работает, генерация ответов работает правильно
**Риск:** Поломка = генерация ответов перестанет работать как нужно

**НЕ МЕНЯТЬ:**
- ❌ Структуру системного промпта без обсуждения
- ❌ Запреты на LaTeX формулы (критично для детей)
- ❌ Адаптацию под возраст (1-9 класс)
- ❌ Логику работы с фото и аудио

**МОЖНО:**
- ✅ Улучшать формулировки
- ✅ Добавлять новые примеры
- ✅ Расширять список предметов

---

### 6. `bot/services/moderation_service.py` - Модерация контента
**Статус:** ✅ Работает, защищает детей
**Риск:** Поломка = опасный контент детям

**НЕ МЕНЯТЬ:**
- ❌ Проверки безопасности
- ❌ Логику фильтрации
- ❌ Отключать модерацию
- ❌ Убирать проверки по возрасту

**МОЖНО:**
- ✅ Добавлять новые паттерны в `forbidden_patterns.py`
- ✅ Улучшать точность модерации

---

## ✅ ПРАВИЛА ДЛЯ РАЗРАБОТКИ

### Перед изменением любого файла (для разработчиков):

1. **Проверь список выше** - есть ли файл в "НЕ ТРОГАТЬ"?
2. **Прочитай ВЕСЬ файл** - не только место изменения
3. **Пойми назначение** - зачем нужны текущие настройки?
4. **Спроси пользователя** - если файл критичен, уточни необходимость изменения

### Если файл в списке "НЕ ТРОГАТЬ":

```
❌ НЕ меняй без разрешения пользователя
✅ Спроси: "Этот файл критичен для работы. Действительно нужно изменить?"
✅ Предложи альтернативу без изменения критичного кода
✅ Если изменение необходимо - делай МИНИМАЛЬНОЕ изменение
```

### При сомнениях:

- **Лучше спросить**, чем сломать
- **Лучше добавить новый код**, чем менять рабочий
- **Лучше расширить функциональность**, чем рефакторить критичное

### Тестирование после изменений:

1. Запусти локально: `python web_server.py`
2. Проверь, что бот запускается без ошибок
3. Проверь, что frontend открывается
4. Проверь консоль браузера на ошибки
5. Проверь логи Railway после деплоя

---

## 📋 Чек-лист перед коммитом критичных изменений

- [ ] Файл не в списке "НЕ ТРОГАТЬ"?
- [ ] Понимаю, что делает текущий код?
- [ ] Изменение действительно необходимо?
- [ ] Есть ли альтернатива без изменения критичного кода?
- [ ] Сохранена ли обратная совместимость?
- [ ] Как это влияет на безопасность детей?
- [ ] Как это влияет на production uptime?
- [ ] Протестировано локально?
- [ ] Нет ошибок в консоли?

---

## 🚫 Запрещённые действия

```
❌ Удаление проверок модерации
❌ Изменение сигнатур публичных методов без необходимости
❌ Отключение логирования
❌ Хардкод секретов
❌ Изменение database_url валидации
❌ Удаление rate limiting
❌ Обход Pydantic валидации
❌ Удаление 'unsafe-inline' из CSP (нужно для Telegram)
❌ Добавление PWA мета-тегов обратно
❌ Изменение favicon настройки без необходимости
```

---

## 📝 Как вносить изменения безопасно

### Новый функционал

```python
# ✅ Создай НОВЫЙ файл/класс
# bot/services/my_new_feature.py
class MyNewFeature:
    pass

# ✅ Зарегистрируй в существующей структуре
# bot/handlers/__init__.py
from bot.handlers.my_feature import router as my_feature_router
routers.append(my_feature_router)
```

### Исправление бага

```python
# ✅ Минимальное изменение с сохранением поведения
def existing_function(param: str) -> str:
    # Баг: не обрабатывался пустой param
    if not param:  # ← Добавлено
        return ""  # ← Добавлено
    return process(param)
```

### Рефакторинг

```python
# ❌ НЕ рефакторь критический код "для красоты"
# ❌ НЕ меняй рабочий код без конкретной причины
# ✅ Рефакторь только при необходимости
# ✅ Сохраняй обратную совместимость
```

---

## 🔄 Откат изменений

Если что-то сломалось:

1. Проверь логи Railway: `railway logs`
2. Откати коммит: `git revert HEAD`
3. Редеплой: `git push origin main`
4. Проверь, что все работает

---

**Последнее обновление:** 2026-01-02
**Статус:** Все перечисленные модули работают стабильно ✅
