# 🔍 Проверка Postgres сервиса в Railway

## Как проверить, что Postgres привязан и называется правильно

### Шаг 1: Откройте Railway Dashboard

1. Зайдите на https://railway.app/dashboard
2. Выберите ваш проект **PandaPal**

### Шаг 2: Проверьте сервисы

В левой панели вы увидите список сервисов:
- **Ваш основной сервис** (например, `pandapal-bot`)
- **Postgres** (или другое имя)

### Шаг 3: Проверьте имя Postgres сервиса

**Важно:** Имя сервиса должно быть точно `Postgres` (с большой буквы P)

Если имя другое:
1. Нажмите на сервис Postgres
2. Перейдите в **Settings**
3. Измените **Service Name** на `Postgres`
4. Или обновите переменную `DATABASE_URL` с правильным именем

### Шаг 4: Проверьте переменную DATABASE_URL

В вашем основном сервисе:
1. Перейдите в **Variables**
2. Найдите `DATABASE_URL`
3. Должно быть: `${{Postgres.RAILWAY_PRIVATE_DOMAIN}}` или `${{Postgres.DATABASE_URL}}`

**Правильные варианты:**
```bash
# Вариант 1: Использовать RAILWAY_PRIVATE_DOMAIN
DATABASE_URL="postgresql://postgres:password@${{Postgres.RAILWAY_PRIVATE_DOMAIN}}:5432/railway"

# Вариант 2: Использовать готовый DATABASE_URL (РЕКОМЕНДУЕТСЯ)
DATABASE_URL="${{Postgres.DATABASE_URL}}"
```

### Шаг 5: Проверьте в логах

После деплоя проверьте логи:
```bash
# В логах должно быть:
✅ Подключение к БД: postgresql+psycopg://***:***@postgres.railway.internal:5432/railway
```

Если видите ошибку подключения - проверьте имя сервиса.

### Шаг 6: Альтернативный способ - через Railway CLI

```bash
# Установите Railway CLI
npm i -g @railway/cli

# Войдите
railway login

# Проверьте сервисы
railway service list

# Проверьте переменные
railway variables
```

## 🔧 Если Postgres не привязан

1. В Railway Dashboard → ваш проект
2. Нажмите **+ New** → **Database** → **Add PostgreSQL**
3. Railway автоматически создаст сервис `Postgres`
4. Переменная `DATABASE_URL` будет автоматически добавлена

## ✅ Итоговая проверка

После проверки убедитесь, что:
- [ ] Postgres сервис существует в проекте
- [ ] Имя сервиса: `Postgres` (или обновлен DATABASE_URL)
- [ ] `DATABASE_URL` использует `${{Postgres.XXX}}`
- [ ] В логах нет ошибок подключения к БД
