# 📊 Руководство по бизнес-метрикам PandaPal

## ✅ Статус: Включено 24/7

Метрики **автоматически записываются** при каждом событии:
- ✅ Заблокированное сообщение → метрика безопасности
- ✅ Успешное AI взаимодействие → метрика образования
- ✅ Просмотр дашборда родителем → метрика родителя

**Работает автоматически**, не требует настройки.

---

## 🔍 Где смотреть метрики

### 1. **Через скрипт (рекомендуется)**

```bash
# Активируйте виртуальное окружение
.\venv\Scripts\Activate.ps1  # Windows PowerShell
# или
source venv/bin/activate      # Linux/Mac

# Все метрики за последние 7 дней
python scripts/view_analytics_metrics.py

# Только метрики безопасности
python scripts/view_analytics_metrics.py --type safety

# Метрики за 30 дней
python scripts/view_analytics_metrics.py --days 30

# Только метрики образования
python scripts/view_analytics_metrics.py --type education

# Только метрики родителей
python scripts/view_analytics_metrics.py --type parent
```

**Вывод:**
```
📊 ПРОСМОТР БИЗНЕС-МЕТРИК PANDAPAL
============================================================
Период: последние 7 дней

🛡️ МЕТРИКИ БЕЗОПАСНОСТИ
============================================================
Заблокированных сообщений за 7 дней: 15
По категориям:
  • basic_moderation: 8
  • violence: 5
  • adult_content: 2

📚 МЕТРИКИ ОБРАЗОВАНИЯ
============================================================
AI взаимодействий за 7 дней: 342
Активных детей: 28
Среднее взаимодействий на ребенка: 12.2

👨‍👩‍👧 МЕТРИКИ РОДИТЕЛЕЙ
============================================================
Просмотров дашборда за 7 дней: 45
Активных родителей: 12
```

### 2. **Через SQL запросы**

Подключитесь к PostgreSQL и выполните:

```sql
-- Все метрики за сегодня
SELECT
    metric_type,
    metric_name,
    SUM(metric_value) as total,
    COUNT(*) as count
FROM analytics_metrics
WHERE DATE(timestamp) = CURRENT_DATE
GROUP BY metric_type, metric_name
ORDER BY metric_type, metric_name;

-- Метрики безопасности за неделю
SELECT
    metric_name,
    SUM(metric_value) as total,
    tags->>'category' as category
FROM analytics_metrics
WHERE metric_type = 'safety'
  AND timestamp >= NOW() - INTERVAL '7 days'
GROUP BY metric_name, tags->>'category'
ORDER BY total DESC;

-- Топ активных детей (по AI взаимодействиям)
SELECT
    user_telegram_id,
    SUM(metric_value) as interactions
FROM analytics_metrics
WHERE metric_type = 'education'
  AND metric_name = 'ai_interactions'
  AND timestamp >= NOW() - INTERVAL '7 days'
GROUP BY user_telegram_id
ORDER BY interactions DESC
LIMIT 10;
```

### 3. **Экспорт метрик**

```bash
# Экспорт в CSV за 7 дней
python scripts/export_metrics.py --format csv --days 7

# Экспорт в JSON за 30 дней
python scripts/export_metrics.py --format json --days 30

# Указать путь вывода
python scripts/export_metrics.py --format csv --days 7 --output metrics/exports/my_metrics.csv
```

Файлы сохраняются в `metrics/exports/` по умолчанию.

### 4. **Через Python код**

```python
from bot.database import get_db
from bot.services.analytics_service import AnalyticsService

with get_db() as db:
    analytics = AnalyticsService(db)

    # Записать метрику вручную (если нужно)
    analytics.record_safety_metric(
        metric_name="custom_metric",
        value=1.0,
        user_telegram_id=123456789
    )
```

---

## 📈 Какие метрики собираются

### 🛡️ Безопасность (`safety`)
- `blocked_messages` — заблокированные сообщения
  - Теги: `category` (basic_moderation, violence, adult_content, etc.)

### 📚 Образование (`education`)
- `ai_interactions` — успешные AI взаимодействия
  - Теги: `subject` (если будет добавлено)

### 👨‍👩‍👧 Родители (`parent`)
- `dashboard_views` — просмотры дашборда родителями

### ⚙️ Технические (`technical`)
- Пока не используются, но готовы к использованию

---

## 🔄 Автоматическая запись

Метрики записываются **автоматически** в следующих местах:

1. **`bot/handlers/ai_chat.py`**:
   - При блокировке сообщения (базовая модерация)
   - При блокировке сообщения (продвинутая модерация)
   - При успешном AI ответе

2. **`bot/handlers/parent_dashboard.py`**:
   - При просмотре дашборда родителем

**Важно:** Все записи в `try/except` — ошибки не ломают основной функционал.

---

## 📊 Агрегация данных

Метрики записываются как **события** (по 1 на событие). Для агрегации используйте SQL:

```sql
-- Заблокированные сообщения по дням
SELECT
    DATE(timestamp) as date,
    SUM(metric_value) as blocked_count
FROM analytics_metrics
WHERE metric_type = 'safety'
  AND metric_name = 'blocked_messages'
  AND timestamp >= NOW() - INTERVAL '30 days'
GROUP BY DATE(timestamp)
ORDER BY date DESC;
```

---

## 🎯 Примеры использования

### Мониторинг безопасности
```bash
python scripts/view_analytics_metrics.py --type safety --days 1
```

### Анализ образовательной активности
```bash
python scripts/view_analytics_metrics.py --type education --days 30
```

### Проверка активности родителей
```bash
python scripts/view_analytics_metrics.py --type parent --days 7
```

---

## ⚠️ Важно

- Метрики записываются **автоматически** — ничего настраивать не нужно
- Работают **24/7** — записываются при каждом событии
- Ошибки записи **не ломают** основной функционал (graceful degradation)
- Данные хранятся в таблице `analytics_metrics` в PostgreSQL

---

## 🔗 Связанные файлы

- `bot/services/analytics_service.py` — сервис записи метрик
- `bot/models.py` — модель `AnalyticsMetric`
- `scripts/view_analytics_metrics.py` — скрипт просмотра
- `scripts/export_metrics.py` — скрипт экспорта в CSV/JSON
- `bot/handlers/ai_chat.py` — интеграция в AI чат
- `bot/handlers/parent_dashboard.py` — интеграция в дашборд
- `metrics/` — папка для работы с метриками (экспорты, отчеты, дашборды)
