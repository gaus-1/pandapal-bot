# 🔴 КРАТКАЯ ИНСТРУКЦИЯ: Redis для сессий

## ✅ ЧТО СДЕЛАНО

Redis интегрирован в проект для **персистентного хранения сессий** веб-пользователей (Telegram Login Widget).

### **Как работает сейчас:**

- ✅ **Без Redis:** Сессии в памяти (теряются при перезапуске)
- ✅ **С Redis:** Сессии персистентны (не теряются)
- ✅ **Автоматический fallback:** Если Redis недоступен → in-memory

---

## 🚀 КАК ПОДКЛЮЧИТЬ (ОПЦИОНАЛЬНО)

### **Вариант 1: Upstash Redis (БЕСПЛАТНО, рекомендуется)**

1. Зарегистрируйся: https://upstash.com
2. Создай базу → **Free plan** (10K команд/день)
3. Скопируй **REDIS_URL** (начинается с `rediss://...`)
4. Добавь в Railway Variables:
   ```bash
   REDIS_URL=rediss://default:AbC123...@us1-mantis-12345.upstash.io:12345
   ```
5. Перезапусти контейнер → готово!

### **Вариант 2: Оставить как есть (in-memory)**

Ничего делать не нужно! Проект работает без Redis.

**Минус:** При перезапуске пользователи потеряют сессии.

---

## 📊 ПРОВЕРКА

### **Логи Railway после перезапуска:**

✅ **С Redis:**
```
✅ Redis подключен для хранения сессий
```

⚠️ **Без Redis (in-memory):**
```
⚠️ Сессии хранятся в памяти (Redis не настроен)
```

---

## 📚 ПОДРОБНАЯ ДОКУМЕНТАЦИЯ

См. `docs/REDIS_SETUP.md` для:
- Пошаговой инструкции
- Альтернативных провайдеров
- FAQ и troubleshooting

---

**Важно:** Redis НЕ обязателен для работы проекта!
