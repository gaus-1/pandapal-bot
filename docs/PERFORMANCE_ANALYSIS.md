# 📊 Анализ производительности при нагрузке 100 одновременных пользователей

**Дата:** 02.01.2026
**Сценарий:** 100 пользователей одновременно отправляют фото, аудио и текстовые запросы в мини-апп и на сайте

## 🔍 Текущее состояние системы

### ✅ Сильные стороны

1. **Асинхронная архитектура**
   - aiohttp сервер (async/await)
   - aiogram 3.x полностью async
   - SQLAlchemy 2.0 async режим
   - Все обработчики async

2. **Пул соединений БД**
   - 5 базовых + 15 overflow = 20 соединений
   - Pool pre-ping для проверки соединений
   - Pool recycle каждые 30 минут

3. **Лимиты запросов**
   - `client_max_size: 10MB` для медиа
   - Rate limiting на уровне пользователя (30 запросов/мин)

### ⚠️ Потенциальные узкие места

## 🚨 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. **Пул соединений БД недостаточен**

**Текущее:**
- `pool_size: 5`
- `max_overflow: 15`
- **Итого: 20 соединений**

**Проблема:**
- 100 пользователей → 100 одновременных запросов
- Каждый запрос требует БД (получение пользователя, сохранение истории)
- **20 соединений < 100 запросов** → очередь ожидания

**Влияние:**
- Задержки 1-5 секунд при ожидании соединения
- Таймауты при `pool_timeout: 30` секунд

**Решение:**
```python
# bot/database.py
pool_kwargs = {
    "pool_size": 20,        # Увеличить базовый пул
    "max_overflow": 30,     # Больше overflow
    "pool_timeout": 60,     # Увеличить таймаут
}
# Итого: 50 соединений (достаточно для 100 пользователей)
```

### 2. **Нет очереди для AI запросов**

**Текущее:**
- Все AI запросы идут параллельно к Yandex Cloud
- Нет ограничения на количество одновременных запросов

**Проблема:**
- 100 пользователей → 100 одновременных запросов к YandexGPT
- Yandex Cloud может иметь лимиты на количество одновременных запросов
- Риск rate limiting от Yandex Cloud API

**Влияние:**
- Ошибки 429 (Too Many Requests) от Yandex Cloud
- Задержки ответов AI
- Непредсказуемое поведение

**Решение:**
```python
# Добавить очередь для AI запросов
from asyncio import Semaphore

class AIRequestQueue:
    def __init__(self, max_concurrent: int = 10):
        self.semaphore = Semaphore(max_concurrent)

    async def process_request(self, func, *args, **kwargs):
        async with self.semaphore:
            return await func(*args, **kwargs)
```

### 3. **Обработка медиа блокирующая**

**Текущее:**
- SpeechKit STT: синхронный HTTP запрос (requests.post)
- Vision OCR: синхронный HTTP запрос
- Нет очереди для медиа-обработки

**Проблема:**
- 30 пользователей отправляют фото → 30 одновременных запросов к Vision
- 20 пользователей отправляют аудио → 20 одновременных запросов к SpeechKit
- Медленные API (2-5 секунд на запрос)

**Влияние:**
- Задержки 5-10 секунд для медиа-запросов
- Блокировка event loop при синхронных запросах

**Решение:**
```python
# Использовать httpx (async) вместо requests
import httpx

async def transcribe_voice_async(audio_data: bytes):
    async with httpx.AsyncClient(timeout=30.0) as client:
        response = await client.post(url, data=audio_data)
        return response.json()
```

### 4. **MemoryStorage для FSM**

**Текущее:**
- `MemoryStorage()` - хранит состояние в памяти
- При рестарте Railway - потеря состояния

**Проблема:**
- При высокой нагрузке может быть проблема с памятью
- Нет персистентности состояния

**Влияние:**
- Потеря состояния при рестарте
- Потенциальные проблемы с памятью

**Решение:**
- Использовать Redis для FSM (если доступен)
- Или оставить MemoryStorage (для простоты)

### 5. **Нет rate limiting на уровне API**

**Текущее:**
- Rate limiting только на уровне пользователя (30 запросов/мин)
- Нет глобального rate limiting для API endpoints

**Проблема:**
- Один пользователь может отправить 30 запросов одновременно
- Нет защиты от DDoS

**Решение:**
```python
# Добавить middleware для rate limiting
from aiohttp_ratelimit import RateLimiter

app.middlewares.append(RateLimiter(requests_per_second=10))
```

## 📊 Прогноз производительности

### Сценарий: 100 одновременных пользователей

**Текстовые запросы (50 пользователей):**
- ✅ Обработаются быстро (1-3 сек)
- ⚠️ Могут быть задержки при ожидании БД (если пул переполнен)

**Фото запросы (30 пользователей):**
- ⚠️ Задержки 5-10 секунд (Vision API медленный)
- ⚠️ Риск блокировки при синхронных запросах

**Аудио запросы (20 пользователей):**
- ⚠️ Задержки 5-15 секунд (SpeechKit медленный)
- ⚠️ Риск блокировки при синхронных запросах

**Общая оценка:**
- ⚠️ **Система выдержит, но с задержками**
- ⚠️ **Риск таймаутов при пиковой нагрузке**
- ⚠️ **Нужна оптимизация для стабильной работы**

## 🚀 Рекомендации по оптимизации

### Приоритет 1: Критично

1. **Увеличить пул БД**
   ```python
   pool_size: 20
   max_overflow: 30
   # Итого: 50 соединений
   ```

2. **Добавить очередь для AI запросов**
   ```python
   max_concurrent_ai_requests: 10-15
   ```

3. **Перейти на async HTTP клиент**
   ```python
   # Заменить requests на httpx
   pip install httpx
   ```

### Приоритет 2: Важно

4. **Добавить кеширование**
   - Кеш для истории чата (Redis или MemoryCache)
   - Кеш для модерации (избежать повторных проверок)

5. **Оптимизировать запросы к БД**
   - Batch inserts для истории
   - Индексы на часто используемые поля

6. **Добавить мониторинг**
   - Метрики времени ответа
   - Метрики использования пула БД
   - Алерты при высокой нагрузке

### Приоритет 3: Желательно

7. **Горизонтальное масштабирование**
   - Несколько инстансов на Railway
   - Load balancer

8. **Оптимизация медиа-обработки**
   - Сжатие изображений перед отправкой в Vision
   - Кеширование результатов распознавания

## 📈 Ожидаемые результаты после оптимизации

**До оптимизации:**
- Задержки: 5-15 секунд для медиа
- Риск таймаутов: высокий
- Пропускная способность: ~50-70 запросов/сек

**После оптимизации:**
- Задержки: 2-5 секунд для медиа, 1-3 сек для текста
- Риск таймаутов: низкий
- Пропускная способность: ~100+ запросов/сек

## ✅ Вывод

**Текущее состояние:**
- ⚠️ Система **выдержит 100 пользователей**, но с **задержками 5-15 секунд**
- ⚠️ Есть риск **таймаутов** при пиковой нагрузке
- ⚠️ **Нужна оптимизация** для стабильной работы

**Рекомендация:**
1. **Немедленно:** Увеличить пул БД до 50 соединений
2. **Срочно:** Добавить очередь для AI запросов (max 10-15 одновременных)
3. **Важно:** Перейти на async HTTP клиент (httpx)
4. **Желательно:** Добавить кеширование и мониторинг

После этих оптимизаций система будет **стабильно работать** с 100+ одновременными пользователями.
