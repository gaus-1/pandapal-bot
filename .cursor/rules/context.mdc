---
alwaysApply: true
---

# 🐼 PandaPal Bot — Правила разработки

## ⚡ ИНСТРУКЦИИ ДЛЯ AI (ОБЯЗАТЕЛЬНО)

**Эти правила применяются ВСЕГДА. Учитывай их в каждом ответе.**

### Экономия токенов
- **Краткие ответы** — без воды, по делу
- **Минимум комментариев в коде** — только для неочевидных решений
- **Не повторяй код** — показывай только изменения
- **Не объясняй очевидное** — пользователь разработчик
- **Группируй изменения** — один файл = одно изменение если возможно

### Качество НЕ страдает
- Код пиши полноценный и рабочий
- Логику не сокращай
- Type hints и docstrings для публичных функций сохраняй
- Тесты пиши полные

### Документация библиотек
- При работе с библиотеками используй актуальную документацию
- Особенно для: aiogram, SQLAlchemy, Pydantic, React, Yandex Cloud API
- Не гадай API — проверяй через документацию библиотек

### Формат ответов
```
✅ Делаю X потому что Y.     ← Кратко
❌ Сейчас я сделаю X, это нужно потому что... [100 слов]
```

### Git коммиты и пуши (АНГЛИЙСКИЙ ЯЗЫК)
- **Язык:** английский
- **Формат:** `тип: краткое описание` (Conventional Commits)
- **Стиль:** как пишет профессиональный разработчик, естественно и понятно

**⚠️ КРИТИЧЕСКИ ВАЖНО: Запрещенные слова в коммитах:**
- ❌ AI, ASSISTENT, ASSISTANT, CURSOR, LLM, IDE, MCP, CONTEXT7
- ✅ Используй нейтральные формулировки: "generation", "system", "module", "library documentation"
- ✅ Проверяй сообщение коммита перед пушем

**Типы:**
- `feat:` — новая функция
- `fix:` — исправление бага
- `refactor:` — рефакторинг
- `docs:` — документация
- `test:` — тесты
- `chore:` — мелочи (зависимости, конфиги)
- `style:` — форматирование, стиль кода
- `perf:` — оптимизация производительности
- `security:` — исправления безопасности

**Примеры:**
```
✅ feat: add voice message support
✅ fix: handle empty message validation in AI chat
✅ refactor: extract AI logic into separate service
✅ docs: update README with new commands
✅ security: improve content moderation for child safety
✅ perf: optimize database queries for chat history

❌ feat: Add voice message support (слишком формально)
❌ fix: Fixed bug (слишком общо)
❌ Update files (нет типа)
❌ WIP (work in progress не коммитим)
```

---

## Идентичность проекта

**PandaPal** — образовательный Telegram-бот для детей 1-9 классов (6-15 лет) с веб-приложением. Работает 24/7 на Railway.app в webhook режиме.

**Главный приоритет #1:**
1. **Безопасность детей** — защита от опасного контента, модерация, родительский контроль
2. **Профессиональный современный код** — чистый, поддерживаемый, с type hints, тестами
3. **Архитектура** — SOLID принципы, разделение ответственности, масштабируемость
4. **Безопасность** — без ущерба работе проекта и всех его компонентов
5. **Профессиональный UI/UX** — интуитивный интерфейс, отличный пользовательский опыт

## Технологический стек

| Компонент | Технология | Версия |
|-----------|------------|--------|
| Backend | Python, aiohttp, aiogram | 3.13, aiogram 3.x |
| Frontend | React, TypeScript, Vite | React 18+ |
| БД | PostgreSQL | 17 |
| AI | Yandex Cloud | YandexGPT, SpeechKit, Vision |
| Деплой | Railway.app | Webhook mode, 24/7 |

## Архитектура

```
web_server.py  →  Entry point (aiohttp + aiogram webhook)
     ↓
bot/handlers/  →  Обработчики команд (aiogram Routers)
     ↓
bot/services/  →  Бизнес-логика (сервисный слой)
     ↓
bot/database.py →  Сессии БД (get_db() context manager)
     ↓
bot/models.py  →  SQLAlchemy модели
```

## Критические модули (НЕ ЛОМАТЬ)

| Модуль | Риск | Что сломается |
|--------|------|---------------|
| `web_server.py` | 🔴 Критический | Бот недоступен |
| `bot/database.py` | 🔴 Критический | Потеря данных |
| `bot/config/settings.py` | 🔴 Критический | Бот не стартует |
| `bot/services/moderation_service.py` | 🟠 Высокий | Опасный контент детям |
| `bot/config/forbidden_patterns.py` | 🟠 Высокий | Обход модерации |
| `bot/handlers/ai_chat.py` | 🟠 Высокий | Модерация пропущена |
| `bot/services/speech_service.py` | 🟡 Средний | Голос не работает |

**Сигнатуры (НЕ МЕНЯТЬ):**
- `speech_service.transcribe_voice(voice_file_bytes: bytes, language: str = "ru")`
- `ai_service.generate_response(user_message, chat_history, user_age)`

## Правила разработки

### Перед изменениями

1. **Прочитай файл** — не гадай, что в нём
2. **Проверь зависимости** — изменения могут сломать другие модули
3. **Пойми контекст** — это для детей, безопасность критична

### Код

- **Модерация обязательна** — любой пользовательский ввод через `ContentModerationService`
- **БД через context manager** — `with get_db() as db:`
- **Сервисы в bot/services/** — логика не в handlers
- **Валидация через Pydantic** — никаких сырых данных
- **Логирование через loguru** — не print()

### Стиль

- Black (line-length=100)
- isort (profile="black")
- Docstrings для публичных функций
- Type hints обязательны

### Безопасность

- Секреты только из переменных окружения
- PII не хранить без необходимости
- Все внешние данные валидировать

## Команды

```bash
# Локальный запуск
python web_server.py

# Тесты
pytest tests/ -v

# Покрытие
pytest tests/ --cov=bot --cov-report=html

# Линтеры
black bot/ && isort bot/ && flake8 bot/
```

## Не делай

❌ Хардкод секретов
❌ Изменение сигнатур публичных методов без согласования
❌ Обход модерации
❌ Polling вместо webhook в продакшене
❌ `print()` вместо `logger`
❌ Прямые SQL запросы (только через SQLAlchemy)

## Делай

✅ **Безопасность детей** — проверяй влияние на безопасность детей перед любыми изменениями
✅ **Профессиональный код** — читай код перед изменениями, используй type hints, пиши тесты
✅ **Архитектура** — следуй SOLID принципам, разделяй ответственность, используй существующие паттерны
✅ **Безопасность системы** — валидируй все входные данные, не ломай существующую функциональность
✅ **UI/UX** — создавай интуитивный интерфейс, думай о пользовательском опыте
✅ Документируй неочевидные решения

## Деплой (Railway.app)

- Push в `main` → Railway автоматически деплоит
- **Webhook:** `pandapal-bot-production.up.railway.app`
- **Frontend:** `pandapal.ru` (Cloudflare DNS)
- **Переменные:** `DATABASE_URL`, `TELEGRAM_BOT_TOKEN`, `YANDEX_*`, `SECRET_KEY`
- **Режим:** webhook (не polling), `sleepApplication: false`

---

**📌 Напоминание:** Эти правила применяются автоматически (alwaysApply: true). Для критического кода см. `critical-code-protection.mdc`.
