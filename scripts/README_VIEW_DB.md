# 📊 Просмотр данных в PostgreSQL

Утилита для просмотра данных в таблицах и проверки активности PostgreSQL.

## 🚀 Быстрый старт

```bash
# Активируйте виртуальное окружение (если нужно)
# Windows:
venv\Scripts\activate

# Linux/Mac:
source venv/bin/activate

# Установите зависимости (если еще не установлены)
pip install tabulate

# Просмотр статистики по всем таблицам
python scripts/view_database.py

# Просмотр данных из конкретной таблицы
python scripts/view_database.py --table users
python scripts/view_database.py --table chat_history --limit 10

# Проверка активности PostgreSQL
python scripts/view_database.py --activity

# Активность за последние 24 часа
python scripts/view_database.py --recent 24
```

## 📋 Доступные команды

### Статистика по таблицам
```bash
python scripts/view_database.py --stats
```
Показывает количество записей в каждой таблице.

### Просмотр данных из таблицы
```bash
python scripts/view_database.py --table <название_таблицы> [--limit N]
```

**Доступные таблицы:**
- `users` - Пользователи
- `chat_history` - История чата
- `learning_sessions` - Учебные сессии
- `user_progress` - Прогресс пользователей
- `analytics_metrics` - Аналитические метрики
- `user_sessions` - Пользовательские сессии
- `user_events` - События пользователей
- `analytics_reports` - Аналитические отчеты
- `analytics_trends` - Тренды аналитики
- `analytics_alerts` - Алерты
- `analytics_config` - Конфигурация аналитики

### Активность PostgreSQL
```bash
python scripts/view_database.py --activity
```
Показывает:
- Размер базы данных
- Количество активных подключений
- Размеры таблиц
- Статистику операций (INSERT, UPDATE, DELETE)

### Недавняя активность
```bash
python scripts/view_database.py --recent <часы>
```
Показывает активность за последние N часов.

## 🔍 Примеры использования

```bash
# Сколько пользователей в базе?
python scripts/view_database.py --table users

# Последние 50 сообщений
python scripts/view_database.py --table chat_history --limit 50

# Проверка размера БД и активности
python scripts/view_database.py --activity

# Активность за последние 12 часов
python scripts/view_database.py --recent 12
```

## ⚠️ Требования

- Python 3.13+
- Установленные зависимости из `requirements.txt`
- Настроенный `.env` файл с `DATABASE_URL`
- Подключение к PostgreSQL

## 📝 Примечания

- Скрипт использует `get_db()` context manager для безопасной работы с БД
- Все запросы выполняются в режиме только чтения
- Для больших таблиц используйте `--limit` для ограничения вывода
