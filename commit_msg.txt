feat(ai): remove markdown formatting for human-readable responses

Prompt changes:
- Add explicit ban on markdown formatting (**, __, *, _)
- Prohibit bold and italic text in responses

Response processing:
- Strip markdown headers (###, ##, #)
- Remove bold formatting (**text**, __text__)
- Remove italic formatting (*text*, _text*)
- Preserve natural text structure with clean formatting

Ensures responses are clean, structured, and human-readable without markdown artifacts.
