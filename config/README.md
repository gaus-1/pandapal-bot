# 📁 CONFIG - Конфигурационные файлы

Все файлы настройки проекта собраны здесь для удобства.

## Файлы:

### **alembic.ini**
Конфигурация миграций базы данных Alembic.

### **pytest.ini**
Настройки тестового фреймворка pytest.

### **pyproject.toml**
Метаданные проекта Python (PEP 518).

### **pyrightconfig.json**
Настройки статического анализатора Pyright (VS Code).

### **env.template**
Шаблон переменных окружения для локальной разработки.

---

## ⚠️ Важно:

**Реальный `.env` файл НЕ в этой папке!**
Он остается в корне проекта (стандарт) и игнорируется git.
